cmake_minimum_required(VERSION 3.25)

project(
        ovumjitx64
        VERSION 0.1
        DESCRIPTION "JIT compiler for Ovum programming language for x86_64 architecture written in C++"
        LANGUAGES CXX
)

add_subdirectory(jit)
